spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: http://localhost:8888
      username: internaluser
      password: internaluser
      fail-fast: true
  profiles:
    active: dev